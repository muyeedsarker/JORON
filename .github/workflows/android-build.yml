name: Android build

on:
  push:
    branches: [ "feature/android-project", "feature/android-firebase-complete-v2" ]
  pull_request:
    branches: [ "feature/android-project" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      # Build directly with the configured Gradle distribution; the repository does not require a checked-in wrapper.
      - name: Verify Android project
        run: gradle -p android tasks --all --stacktrace --no-daemon

      - name: Build debug APK
        run: gradle -p android assembleDebug --stacktrace --no-daemon

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: joron-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
